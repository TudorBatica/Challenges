name: Flutter CI

on:
  push:
    branches:
      - master

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: subosito/flutter-action@v1

      - run: flutter pub get

      - run: flutter test

      - run: flutter build web
